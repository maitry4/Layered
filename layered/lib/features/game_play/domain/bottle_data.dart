/// Pure data — no logic. Holds what fruits are inside a bottle (bottom → top).
class BottleData {
  final List<String> fruits; // e.g. ['strawberry', 'watermelon', 'lime']

  const BottleData({required this.fruits});
}