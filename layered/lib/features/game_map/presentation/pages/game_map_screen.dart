import 'package:flutter/material.dart';

class GameMapScreen extends StatelessWidget {
  const GameMapScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child:Text("Game Map")),
    );
  }
}